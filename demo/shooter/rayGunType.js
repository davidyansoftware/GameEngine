const fastRayGun = {
    length: 200,
    
    cooldown: .1,
    recoilSpeed: 20,

    duration: 0
}

const normalRayGun = {
    length: 200,
    
    cooldown: .5,
    recoilSpeed: 30,

    duration: 0
};

const slowRayGun = {
    length: 300,

    cooldown: 1,
    recoilSpeed: 50,

    duration: 0
}
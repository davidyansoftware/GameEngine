//TODO these should be frozen

const fastGun = {
    bulletRadius: 2,
    bulletSpeed: 1000,
    bulletMaxDistance: 20000,

    cooldown: .25,
    recoilSpeed: 20,
    recoilDistance: 20
}

const normalGun = {
    bulletRadius: 5,
    bulletSpeed: 700,
    bulletMaxDistance: 20000,

    cooldown: .5,
    recoilSpeed: 20,
    recoilDistance: 20
}

const slowGun = {
    bulletRadius: 10,
    bulletSpeed: 500,
    bulletMaxDistance: 20000,

    cooldown: 1,
    recoilSpeed: 20,
    recoilDistance: 20
}

const rocketEngine = {
    bulletRadius: 10,
    bulletSpeed: 1000,
    bulletMaxDistance: 1000,

    cooldown: .05,
    recoilSpeed: 100,
    recoilDistance: 20
}